meta {
  name: Get Majors by School
  type: graphql
  seq: 4
}

post {
  url: {{gql_url}}
  body: graphql
  auth: none
}

body:graphql {
  query {
    majorsBySchool(schoolId: "{{school_id}}") {
      id
      name
      code
    }
  }
}
