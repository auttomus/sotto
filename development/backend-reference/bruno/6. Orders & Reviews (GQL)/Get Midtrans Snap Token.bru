meta {
  name: Get Midtrans Snap Token
  type: graphql
  seq: 12
}

post {
  url: {{gql_url}}
  body: graphql
  auth: inherit
}

body:graphql {
  mutation {
    getMidtransSnapToken(orderId: "{{order_id}}")
  }
}
