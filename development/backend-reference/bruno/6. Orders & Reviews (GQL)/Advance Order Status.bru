meta {
  name: Advance Order Status
  type: graphql
  seq: 2
}

post {
  url: {{gql_url}}
  body: graphql
  auth: inherit
}

body:graphql {
  mutation {
    advanceOrderStatus(orderId: "{{order_id}}") {
      id
      status
      updatedAt
    }
  }
}
