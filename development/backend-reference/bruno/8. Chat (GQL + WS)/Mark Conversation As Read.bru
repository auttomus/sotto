meta {
  name: Mark Conversation As Read
  type: graphql
  seq: 9
}

post {
  url: {{gql_url}}
  body: graphql
  auth: inherit
}

body:graphql {
  mutation {
    markConversationAsRead(conversationId: "{{conversation_id}}")
  }
}
